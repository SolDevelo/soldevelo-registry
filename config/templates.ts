// Autogenerated by lib/build-registry.mts. Run `pnpm registry:build`; do not edit.
// "server-only" keeps the embedded source out of every client bundle.
import "server-only"

import type { RegistryEntry } from "@/lib/types"

export const templates: RegistryEntry[] = [
  {
    "kind": "template",
    "name": "openlmis-list-page",
    "project": "openlmis",
    "title": "List Page",
    "height": "783px",
    "description": "Complete server-paged list screen, shown with OpenLMIS users on mock data: breadcrumbs and heading, a toolbar with search, a status filter, a View menu and Add User, a table with sorting, paging, loading, empty, no-matches and error states, and row actions that open Edit User and Reset Password dialogs.",
    "registryDependencies": [
      "button",
      "dropdown-menu",
      "https://registry.soldevelo.com/r/openlmis-column-view-options.json",
      "https://registry.soldevelo.com/r/openlmis-data-table.json",
      "https://registry.soldevelo.com/r/openlmis-list-toolbar.json",
      "https://registry.soldevelo.com/r/openlmis-page-breadcrumbs.json",
      "https://registry.soldevelo.com/r/openlmis-reset-password-dialog.json",
      "https://registry.soldevelo.com/r/openlmis-search-input.json",
      "https://registry.soldevelo.com/r/openlmis-select-filter.json",
      "https://registry.soldevelo.com/r/openlmis-status-badge.json",
      "https://registry.soldevelo.com/r/openlmis-user-form-dialog.json",
      "https://registry.soldevelo.com/r/openlmis-workspace.json"
    ],
    "dependencies": [
      "@tanstack/react-table@^9",
      "lucide-react"
    ],
    "files": [
      {
        "type": "page",
        "name": "page.tsx",
        "code": "import { ListPage } from \"@/components/openlmis/list-page/list-page\"\n\n// A Next.js App Router route; in any other React app, render ListPage from your own route.\nexport default function Page() {\n  return <ListPage />\n}",
        "lang": "tsx",
        "target": "app/list-page/page.tsx"
      },
      {
        "type": "component",
        "name": "list-page.tsx",
        "code": "\"use client\"\n\nimport {\n  type ColumnVisibilityState,\n  functionalUpdate,\n  useTable,\n} from \"@tanstack/react-table\"\nimport { PlusIcon, SearchXIcon, UsersIcon } from \"lucide-react\"\nimport { useCallback, useMemo, useState } from \"react\"\n\nimport { Button } from \"@/components/ui/button\"\nimport { PageBreadcrumbs } from \"@/components/openlmis/page-breadcrumbs\"\nimport {\n  DataTable,\n  DataTableEmpty,\n  DataTableError,\n  DataTablePagination,\n  DataTableSkeleton,\n  dataTableFeatures,\n} from \"@/components/blocks/openlmis/data-table/data-table\"\nimport {\n  useColumnVisibility,\n  useContainerSize,\n} from \"@/components/blocks/openlmis/data-table/responsive-columns\"\nimport {\n  ListToolbar,\n  ListToolbarEnd,\n  ListToolbarFilter,\n  ListToolbarSearch,\n} from \"@/components/blocks/openlmis/list-toolbar\"\nimport {\n  Workspace,\n  WorkspaceContent,\n  WorkspaceDescription,\n  WorkspaceHeader,\n  WorkspaceHeading,\n  WorkspaceIcon,\n  WorkspaceTitle,\n} from \"@/components/blocks/openlmis/workspace\"\nimport { ColumnViewOptions } from \"@/components/openlmis/column-view-options\"\nimport { SearchInput } from \"@/components/openlmis/search-input/search-input\"\nimport { SelectFilter } from \"@/components/openlmis/select-filter\"\nimport {\n  type PasswordDialogTarget,\n  ResetPasswordDialog,\n} from \"@/components/blocks/openlmis/reset-password-dialog/reset-password-dialog\"\nimport {\n  UserFormDialog,\n  type UserFormDialogTarget,\n} from \"@/components/blocks/openlmis/user-form-dialog/user-form-dialog\"\n\nimport {\n  fetchFacilities,\n  fetchUser,\n  saveUser,\n  sendResetEmail,\n  setPassword,\n  type User,\n  type UsersQuery,\n} from \"@/components/openlmis/list-page/mock-users\"\nimport { createUserColumns, HIDEABLE_COLUMNS } from \"@/components/openlmis/list-page/user-columns\"\nimport { useUserList } from \"@/components/openlmis/list-page/use-user-list\"\n\nconst NO_USERS: User[] = []\n\nconst getRowId = (user: User) => user.id\n\n// One dialog at a time: adding a user hands over to Set Password rather than stacking a second dialog.\ntype OpenDialog =\n  | { kind: \"user\"; target: UserFormDialogTarget }\n  | { kind: \"password\"; target: PasswordDialogTarget }\n\n/** The whole users list screen; mount it from any route, e.g. the `page.tsx` this template ships. */\nexport function ListPage() {\n  const list = useUserList()\n  const { query, update, retry: refresh } = list\n  const [dialog, setDialog] = useState<OpenDialog>()\n  const closeDialog = () => setDialog(undefined)\n  // Stable, so the columns built from them keep their identity between renders.\n  const onEdit = useCallback(\n    (userId: string) => setDialog({ kind: \"user\", target: userId }),\n    []\n  )\n  const onResetPassword = useCallback(\n    (userId: string) =>\n      setDialog({ kind: \"password\", target: { userId, created: false } }),\n    []\n  )\n  const columns = useMemo(\n    () => createUserColumns({ onEdit, onResetPassword }),\n    [onEdit, onResetPassword]\n  )\n  const [measureContent, contentSize] = useContainerSize<HTMLDivElement>()\n  // Kept for the visit only; store it (e.g. in localStorage) to remember it across visits.\n  const columnChoices = useState<ColumnVisibilityState>({})\n  const columnView = useColumnVisibility(\n    HIDEABLE_COLUMNS,\n    columnChoices,\n    contentSize\n  )\n\n  const pagination = { pageIndex: query.pageIndex, pageSize: query.pageSize }\n  // Memoized: the table compares controlled state by reference and would reset it every render.\n  const sorting = useMemo(\n    () => [{ id: query.sortBy, desc: query.sortDesc }],\n    [query.sortBy, query.sortDesc]\n  )\n\n  const table = useTable({\n    features: dataTableFeatures,\n    columns,\n    data: list.data?.rows ?? NO_USERS,\n    getRowId,\n    rowCount: list.data?.total ?? 0,\n    manualPagination: true,\n    manualSorting: true,\n    enableSortingRemoval: false,\n    state: { pagination, sorting, columnVisibility: columnView.visibility },\n    onPaginationChange: (updater) =>\n      update(functionalUpdate(updater, pagination)),\n    onSortingChange: (updater) => {\n      const [next] = functionalUpdate(updater, sorting)\n      if (next)\n        update({ sortBy: next.id as UsersQuery[\"sortBy\"], sortDesc: next.desc })\n    },\n  })\n\n  return (\n    <Workspace>\n      <PageBreadcrumbs\n        items={[\n          { label: \"Home\", href: \"#\" },\n          { label: \"Administration\" },\n          { label: \"Users\" },\n        ]}\n      />\n      <WorkspaceHeader>\n        <WorkspaceHeading>\n          <WorkspaceIcon>\n            <UsersIcon />\n          </WorkspaceIcon>\n          <WorkspaceTitle>Users</WorkspaceTitle>\n          <WorkspaceDescription>\n            Accounts that can sign in to OpenLMIS.\n          </WorkspaceDescription>\n        </WorkspaceHeading>\n      </WorkspaceHeader>\n      <WorkspaceContent>\n        {/* Measured, because the room for columns depends on a sidebar as well as the window. */}\n        <div className=\"flex flex-col gap-4\" ref={measureContent}>\n          <ListToolbar>\n            <ListToolbarSearch>\n              <SearchInput\n                label=\"Search by username, name or email\"\n                onValueChange={(search) => update({ search })}\n                value={query.search}\n              />\n            </ListToolbarSearch>\n            <ListToolbarFilter>\n              <SelectFilter\n                label=\"Status\"\n                onValueChange={(status) =>\n                  update({ status: status as UsersQuery[\"status\"] })\n                }\n                options={[\n                  { value: \"active\", label: \"Active\" },\n                  { value: \"inactive\", label: \"Inactive\" },\n                ]}\n                value={query.status}\n              />\n            </ListToolbarFilter>\n            <ListToolbarEnd>\n              <ColumnViewOptions\n                columns={HIDEABLE_COLUMNS}\n                onReset={columnView.onReset}\n                onVisibilityChange={columnView.onVisibilityChange}\n                visibility={columnView.visibility}\n              />\n              <Button\n                onClick={() => setDialog({ kind: \"user\", target: \"new\" })}\n              >\n                <PlusIcon data-icon=\"inline-start\" />\n                Add User\n              </Button>\n            </ListToolbarEnd>\n          </ListToolbar>\n\n          {list.error ? (\n            <DataTableError\n              description=\"Check your connection and try again.\"\n              onRetry={list.retry}\n              title=\"Could Not Load Users\"\n            />\n          ) : list.data === undefined ? (\n            <DataTableSkeleton rowCount={query.pageSize} table={table} />\n          ) : (\n            <DataTable\n              empty={\n                list.isFiltered ? (\n                  <DataTableEmpty\n                    action={\n                      <Button onClick={list.clearFilters} variant=\"destructive\">\n                        Clear Filters\n                      </Button>\n                    }\n                    description=\"Try a different search or clear the filters.\"\n                    icon={<SearchXIcon />}\n                    title=\"No Matching Users\"\n                  />\n                ) : (\n                  <DataTableEmpty\n                    description=\"Users added to OpenLMIS appear here.\"\n                    icon={<UsersIcon />}\n                    title=\"No Users Yet\"\n                  />\n                )\n              }\n              footer={\n                list.data.total > 0 && <DataTablePagination table={table} />\n              }\n              isStale={list.isStale}\n              table={table}\n            />\n          )}\n        </div>\n      </WorkspaceContent>\n\n      <UserFormDialog\n        loadFacilities={fetchFacilities}\n        loadUser={fetchUser}\n        onClose={closeDialog}\n        onCreated={(userId) =>\n          setDialog({ kind: \"password\", target: { userId, created: true } })\n        }\n        saveUser={async (values, existing) => {\n          const userId = await saveUser(values, existing)\n          refresh()\n          return userId\n        }}\n        target={dialog?.kind === \"user\" ? dialog.target : undefined}\n      />\n      <ResetPasswordDialog\n        loadUser={fetchUser}\n        onClose={closeDialog}\n        sendResetEmail={sendResetEmail}\n        setPassword={setPassword}\n        target={dialog?.kind === \"password\" ? dialog.target : undefined}\n      />\n    </Workspace>\n  )\n}",
        "lang": "tsx",
        "target": "components/openlmis/list-page/list-page.tsx"
      },
      {
        "type": "lib",
        "name": "mock-users.ts",
        "code": "// Stand-in data and server, so the page runs with no backend. Replace each function with a real call.\n\nimport type {\n  Facility,\n  UserDetails,\n  UserFormValues,\n} from \"@/components/blocks/openlmis/user-form-dialog/user-form\"\n\nexport type User = {\n  id: string\n  username: string\n  firstName: string\n  lastName: string\n  email: string | null\n  active: boolean\n}\n\nexport type UsersQuery = {\n  pageIndex: number\n  pageSize: number\n  sortBy: \"username\" | \"lastName\" | \"active\"\n  sortDesc: boolean\n  /** Matches username, first or last name, or email. */\n  search: string\n  status: \"\" | \"active\" | \"inactive\"\n}\n\nexport type UsersPage = {\n  rows: User[]\n  total: number\n}\n\n/** Searching for this shows the error state, so it can be tried without a failing server. */\nconst SIMULATED_FAILURE_SEARCH = \"fail\"\n\nconst FIRST_NAMES = [\n  \"Adora\",\n  \"Alan\",\n  \"Chimango\",\n  \"Grace\",\n  \"Ibrahim\",\n  \"Lindiwe\",\n  \"Mateus\",\n  \"Nia\",\n  \"Omar\",\n  \"Priya\",\n  \"Tendai\",\n  \"Zara\",\n]\nconst LAST_NAMES = [\n  \"Adamovich\",\n  \"Banda\",\n  \"Costa\",\n  \"Diallo\",\n  \"Ehrenfreund\",\n  \"Hopper\",\n  \"Kamau\",\n  \"Mwale\",\n  \"Okafor\",\n  \"Silva\",\n  \"Tebulo\",\n  \"Zulu\",\n]\n\nconst FACILITIES: Facility[] = [\n  { id: \"facility-1\", code: \"HC01\", name: \"Balaka District Hospital\" },\n  { id: \"facility-2\", code: \"HC02\", name: \"Comfort Health Clinic\" },\n  { id: \"facility-3\", code: \"HC03\", name: \"Kankao Health Facility\" },\n  { id: \"facility-4\", code: \"HC04\", name: \"Nandumbo Health Center\" },\n  { id: \"facility-5\", code: \"W001\", name: \"Ntcheu District Warehouse\" },\n]\n\nconst USERS: UserDetails[] = Array.from({ length: 57 }, (_, index) => {\n  const firstName = FIRST_NAMES[index % FIRST_NAMES.length] as string\n  // Offset each round of first names, so no two users share a full name.\n  const lastName = LAST_NAMES[\n    (index + Math.floor(index / FIRST_NAMES.length)) % LAST_NAMES.length\n  ] as string\n  const username = `${firstName[0]}${lastName}${index + 1}`.toLowerCase()\n  const email = index % 3 === 0 ? null : `${username}@example.org`\n  // Only an address can be verified, and notifications need a verified one.\n  const verified = email !== null && index % 2 === 0\n  return {\n    id: `user-${index + 1}`,\n    username,\n    firstName,\n    lastName,\n    email,\n    emailVerified: verified,\n    jobTitle: null,\n    phoneNumber: null,\n    active: index % 4 !== 0,\n    homeFacilityId: FACILITIES[index % FACILITIES.length]?.id ?? null,\n    allowNotify: verified,\n    homeFacilityRoleCount: index % 3,\n  }\n})\n\nconst DELAY_MS = 450\n\nconst wait = () => new Promise<void>((resolve) => setTimeout(resolve, DELAY_MS))\n\nconst toRow = ({ id, username, firstName, lastName, email, active }: User) => ({\n  id,\n  username,\n  firstName,\n  lastName,\n  email,\n  active,\n})\n\nexport async function fetchUsers(query: UsersQuery): Promise<UsersPage> {\n  await wait()\n\n  const term = query.search.trim().toLowerCase()\n  if (term === SIMULATED_FAILURE_SEARCH) {\n    throw new Error(\"The server could not be reached.\")\n  }\n\n  const matches = USERS.filter((user) => {\n    const text = `${user.username} ${user.firstName} ${user.lastName} ${user.email ?? \"\"}`\n    const status =\n      query.status === \"\" || user.active === (query.status === \"active\")\n    return status && text.toLowerCase().includes(term)\n  })\n\n  // oxlint-disable-next-line unicorn/no-array-sort -- sorts a copy; toSorted needs the ES2023 lib\n  const sorted = [...matches].sort((a, b) => {\n    const order = String(a[query.sortBy]).localeCompare(String(b[query.sortBy]))\n    return query.sortDesc ? -order : order\n  })\n\n  const start = query.pageIndex * query.pageSize\n  return {\n    rows: sorted.slice(start, start + query.pageSize).map(toRow),\n    total: matches.length,\n  }\n}\n\nexport async function fetchUser(userId: string): Promise<UserDetails> {\n  await wait()\n  const user = USERS.find((candidate) => candidate.id === userId)\n  if (!user) throw new Error(\"This user no longer exists.\")\n  return user\n}\n\nexport async function fetchFacilities(): Promise<Facility[]> {\n  await wait()\n  return FACILITIES\n}\n\n/** Creates the user, or updates `existing`; resolves to its id, as the dialog expects. */\nexport async function saveUser(\n  values: UserFormValues,\n  existing?: UserDetails\n): Promise<string> {\n  await wait()\n  const taken = USERS.some(\n    (user) => user.username === values.username && user.id !== existing?.id\n  )\n  if (taken) throw new Error(`Username ${values.username} is already taken.`)\n\n  const saved: UserDetails = {\n    id: existing?.id ?? `user-${USERS.length + 1}`,\n    username: values.username,\n    firstName: values.firstName,\n    lastName: values.lastName,\n    email: values.email || null,\n    // A changed address has to be verified again.\n    emailVerified:\n      existing !== undefined &&\n      existing.emailVerified &&\n      existing.email === (values.email || null),\n    jobTitle: values.jobTitle || null,\n    phoneNumber: values.phoneNumber || null,\n    active: values.active,\n    homeFacilityId: values.homeFacilityId,\n    allowNotify: values.allowNotify,\n    homeFacilityRoleCount: values.removeHomeFacilityRoles\n      ? 0\n      : (existing?.homeFacilityRoleCount ?? 0),\n  }\n  const index = USERS.findIndex((user) => user.id === saved.id)\n  if (index === -1) USERS.push(saved)\n  else USERS[index] = saved\n  return saved.id\n}\n\nexport async function sendResetEmail(_email: string): Promise<void> {\n  await wait()\n}\n\nexport async function setPassword(\n  _username: string,\n  _password: string\n): Promise<void> {\n  await wait()\n}",
        "lang": "ts",
        "target": "components/openlmis/list-page/mock-users.ts"
      },
      {
        "type": "hook",
        "name": "use-user-list.ts",
        "code": "\"use client\"\n\nimport { useEffect, useState } from \"react\"\n\nimport { fetchUsers, type UsersPage, type UsersQuery } from \"@/components/openlmis/list-page/mock-users\"\n\nconst INITIAL_QUERY: UsersQuery = {\n  pageIndex: 0,\n  pageSize: 10,\n  sortBy: \"username\",\n  sortDesc: false,\n  search: \"\",\n  status: \"\",\n}\n\n// Each answer records the request it belongs to, so loading is derived rather than stored.\ntype ListResult = {\n  key: string | undefined\n  query: UsersQuery | undefined\n  data: UsersPage | undefined\n  error: Error | undefined\n}\n\n/** List state and its data in plain React; swap in URL search params or React Query as needed. */\nexport function useUserList() {\n  const [query, setQuery] = useState(INITIAL_QUERY)\n  const [attempt, setAttempt] = useState(0)\n  const [result, setResult] = useState<ListResult>({\n    key: undefined,\n    query: undefined,\n    data: undefined,\n    error: undefined,\n  })\n  const key = `${JSON.stringify(query)}#${attempt}`\n\n  useEffect(() => {\n    let current = true\n\n    const load = async () => {\n      try {\n        const data = await fetchUsers(query)\n        if (current) setResult({ key, query, data, error: undefined })\n      } catch (error) {\n        if (current) {\n          setResult({ key, query, data: undefined, error: error as Error })\n        }\n      }\n    }\n    void load()\n\n    // A newer request supersedes this one, so its late answer is ignored.\n    return () => {\n      current = false\n    }\n  }, [key, query])\n\n  const isLoading = result.key !== key\n\n  /** A filter or sort starts again from the first page; paging keeps the rest. */\n  const update = (patch: Partial<UsersQuery>) =>\n    setQuery((previous) => {\n      const next = {\n        ...previous,\n        ...patch,\n        pageIndex: \"pageIndex\" in patch ? (patch.pageIndex ?? 0) : 0,\n      }\n      // An update that changes nothing keeps the same query, so it fetches nothing.\n      const changed = (Object.keys(next) as (keyof UsersQuery)[]).some(\n        (field) => next[field] !== previous[field]\n      )\n      return changed ? next : previous\n    })\n\n  return {\n    query,\n    update,\n    data: result.data,\n    /** Whether the rows on screen came from a filtered request, which picks the empty state. */\n    isFiltered: Boolean(result.query?.search || result.query?.status),\n    error: isLoading ? undefined : result.error,\n    /** Rows are on screen from an earlier request while the next one loads. */\n    isStale: isLoading && result.data !== undefined,\n    retry: () => setAttempt((count) => count + 1),\n    clearFilters: () => update({ search: \"\", status: \"\" }),\n  }\n}",
        "lang": "ts",
        "target": "components/openlmis/list-page/use-user-list.ts"
      },
      {
        "type": "component",
        "name": "user-columns.tsx",
        "code": "\"use client\"\n\nimport { createColumnHelper } from \"@tanstack/react-table\"\nimport {\n  EllipsisIcon,\n  KeyRoundIcon,\n  PencilIcon,\n  ShieldIcon,\n} from \"lucide-react\"\n\nimport { Button } from \"@/components/ui/button\"\nimport {\n  DropdownMenu,\n  DropdownMenuContent,\n  DropdownMenuItem,\n  DropdownMenuTrigger,\n} from \"@/components/ui/dropdown-menu\"\nimport {\n  DataTableColumnHeader,\n  type DataTableFeatures,\n} from \"@/components/blocks/openlmis/data-table/data-table\"\nimport type { ResponsiveColumn } from \"@/components/blocks/openlmis/data-table/responsive-columns\"\nimport { StatusBadge } from \"@/components/openlmis/status-badge\"\n\nimport type { User } from \"@/components/openlmis/list-page/mock-users\"\n\n/** The columns the View menu lists, in order; `hideBelow` drops them while the table is narrow. */\nexport const HIDEABLE_COLUMNS: (ResponsiveColumn & { label: string })[] = [\n  { id: \"lastName\", label: \"Name\", hideBelow: \"xl\" },\n  { id: \"email\", label: \"Email\", hideBelow: \"4xl\" },\n  { id: \"active\", label: \"Status\" },\n]\n\nconst columnHelper = createColumnHelper<DataTableFeatures, User>()\n\n/** What the row menu does; Roles is left for you to wire to a screen of your own. */\nexport type UserRowActions = {\n  onEdit: (userId: string) => void\n  onResetPassword: (userId: string) => void\n}\n\nexport const createUserColumns = (actions: UserRowActions) =>\n  columnHelper.columns([\n    // Shows the full name but sorts by last name, the usual order for a list of people.\n    columnHelper.accessor(\"lastName\", {\n      header: ({ column }) => (\n        <DataTableColumnHeader column={column} title=\"Name\" />\n      ),\n      cell: ({ row }) => `${row.original.firstName} ${row.original.lastName}`,\n      meta: { className: \"@xl/table:w-2/5 @4xl/table:w-1/5\" },\n    }),\n    columnHelper.accessor(\"username\", {\n      header: ({ column }) => (\n        <DataTableColumnHeader column={column} title=\"Username\" />\n      ),\n      cell: ({ getValue }) => <span className=\"font-medium\">{getValue()}</span>,\n      // No width below 4xl, so the username takes the room the hidden columns leave.\n      meta: { className: \"@4xl/table:w-1/6\" },\n    }),\n    columnHelper.accessor(\"email\", {\n      header: ({ column }) => (\n        <DataTableColumnHeader column={column} title=\"Email\" />\n      ),\n      cell: ({ getValue }) =>\n        getValue() ?? <span className=\"text-muted-foreground\">-</span>,\n      enableSorting: false,\n    }),\n    columnHelper.accessor(\"active\", {\n      header: ({ column }) => (\n        <DataTableColumnHeader column={column} title=\"Status\" />\n      ),\n      cell: ({ getValue }) =>\n        getValue() ? (\n          <StatusBadge tone=\"success\">Active</StatusBadge>\n        ) : (\n          <StatusBadge tone=\"destructive\">Inactive</StatusBadge>\n        ),\n      meta: { className: \"w-32\" },\n    }),\n    columnHelper.display({\n      id: \"actions\",\n      header: () => <span className=\"sr-only\">Actions</span>,\n      cell: ({ row }) => (\n        <UserActions\n          onEdit={() => actions.onEdit(row.original.id)}\n          onResetPassword={() => actions.onResetPassword(row.original.id)}\n          username={row.original.username}\n        />\n      ),\n      meta: { className: \"w-16\" },\n    }),\n  ])\n\nfunction UserActions({\n  username,\n  onEdit,\n  onResetPassword,\n}: { username: string } & Record<\"onEdit\" | \"onResetPassword\", () => void>) {\n  return (\n    <div className=\"flex justify-end\">\n      <DropdownMenu>\n        <DropdownMenuTrigger\n          render={\n            <Button\n              aria-label={`Actions for ${username}`}\n              size=\"icon-sm\"\n              variant=\"ghost\"\n            />\n          }\n        >\n          <EllipsisIcon />\n        </DropdownMenuTrigger>\n        <DropdownMenuContent align=\"end\" className=\"w-auto\">\n          <DropdownMenuItem onClick={onEdit}>\n            <PencilIcon />\n            Edit\n          </DropdownMenuItem>\n          <DropdownMenuItem>\n            <ShieldIcon />\n            Roles\n          </DropdownMenuItem>\n          <DropdownMenuItem onClick={onResetPassword} variant=\"destructive\">\n            <KeyRoundIcon />\n            Reset Password\n          </DropdownMenuItem>\n        </DropdownMenuContent>\n      </DropdownMenu>\n    </div>\n  )\n}",
        "lang": "tsx",
        "target": "components/openlmis/list-page/user-columns.tsx"
      }
    ]
  }
]
